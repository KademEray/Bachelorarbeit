# --- CPU / Parallelität ---
max_worker_processes            = 10    # Obergrenze aller Background-Worker
max_parallel_workers            = 10    # globales Budget für Parallel-Worker
max_parallel_workers_per_gather = 5     # pro Gather (≈ Query) – halb so viele wie oben

# --- Speicher (nur ein großer Hebel) ---
shared_buffers                  = 6GB   # ≈ 25 % vom Beispiel-16 GB-Host
